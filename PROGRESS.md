# Progress

- Harness conversion starter created: protected laws, milestone contract, acceptance tests, Codex structured runner, progress health probe, and simulation/Unreal architecture split.
- Codex lifecycle foundation added: explicit workspace-write sandbox, JSONL/stderr separation, persisted thread resume, child PID tracking, last-run metadata, single-instance supervisor with exponential backoff, pause/stop controls, race-safe restart, and static H0 checks.
- Codex PR review findings addressed: atomic runner/supervisor PID locks, streaming thread-id persistence, fully drained event streams, collision-resistant run logs, corrected lifecycle invocation counting, expanded H0 control-plane coverage, and fail-closed gate configuration errors.
- PR #1 timeout-tree review fix: gate escalation now targets the POSIX process group even after its direct leader exits, and the post-SIGKILL pipe drain is bounded/fail-closed. The regression reproduces a leader that exits on TERM plus a descendant that ignores TERM/HUP and retains inherited pipes. `python3 bin/milestone-gate.py --json` passed all five H0 checks on 2026-08-13.
- PR #1 redirected-output follow-up: timeout cleanup now preserves the complete TERM grace period and always sends process-group SIGKILL afterward, even when the leader exits and stdout/stderr reach EOF. A second regression redirects a stubborn descendant's streams to `/dev/null` and proves it cannot perform a delayed worktree write. The complete five-check H0 gate passed again on 2026-08-13.
- PR #1 wrapper/restart follow-up: the Codex process wrapper now preserves its own full TERM grace period before an unconditional process-group SIGKILL, persists its PID, and watches a restart stop-control file. Restart can therefore recover the inherited runner lock after the runner shell is SIGKILLed without signaling recycled PID metadata. Deterministic redirected-descendant and orphaned-wrapper regressions pass; the complete five-check H0 gate passed on 2026-08-13.
- PR #1 zombie-wrapper test follow-up: orphan recovery evidence now verifies that restart installs a distinct live wrapper holding the authoritative runner lock instead of treating `kill -0` signalability as proof the old orphan still executes. This permits an exited zombie awaiting PID 1 reaping without weakening recovery coverage. The focused control-plane suite and complete five-check H0 gate passed on 2026-08-13.
- PR #1 replacement-wrapper timing follow-up: the orphan-recovery regression now polls with a bounded timeout for the replacement Python wrapper after restart reports runner-lock ownership, covering the legitimate shell-to-wrapper startup interval without weakening the distinct-live-wrapper assertion. The focused control-plane suite and complete five-check H0 gate passed on 2026-08-13.

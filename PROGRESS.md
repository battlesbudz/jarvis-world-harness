# Progress

- PR #11 basic-telegraph victory cancellation on 2026-08-31: if a pending Bio strike legitimately defeats the bandit and cancels a basic telegraph, the guard branch now accepts the authoritative victory only after confirming required visible-hit evidence was already captured; non-victory resolutions still require unchanged health/reset and continued engagement.

- PR #11 authoritative basic-block proof on 2026-08-31: strikes now begin only with a 50-stamina reserve, leaving enough for the attack cost, held-block drain, and basic impact. Every short-telegraph defense independently proves unchanged health, unchanged reset identity, and continued engagement after resolution instead of accepting telegraph disappearance as success.

- PR #11 throttled-frame basic defense on 2026-08-31: the deterministic route now holds authoritative guard through the short 580 ms basic telegraph, which hosted capped frames could resolve before ordinary locomotion established separation. Longer heavy and area attacks still require proven physical separation, and the independent controls scenario still exercises stamina drain plus keyboard dodge displacement.

- PR #11 fail-closed combat screenshot proof on 2026-08-31: an uncaptured enemy-health decrease remains pending until the unambiguous `BLOCKED · HALF DAMAGE` player-strike message is visibly rendered, and victory now fails explicitly unless that frame was written, so direct Playwright CI cannot pass without its required combat-feedback PNG.

- PR #11 damage-frame evidence binding on 2026-08-31: visible combat evidence is captured only on a newly observed authoritative enemy-health decrease whose currently rendered feedback is the Bio's exact blocked-hit or guard-break message, preventing a later telegraph or evasion banner from satisfying the evidence contract.

- PR #11 visible-feedback evidence correction on 2026-08-31: each combat profile now captures its required `combat-victory` evidence on the first authoritative damaging hit while the hit feedback is visibly rendered, then records a distinct later `gate-open` image after persistent collision state proves the route is physically available.

- PR #11 hosted range/evidence stabilization on 2026-08-31: the legitimate combat driver now spends stamina only inside the Bio's authoritative closing-step range instead of repeatedly missing after a physical evade. Victory screenshots and assertions wait for persistent enemy-health and physical-gate state while retaining the transient feedback text as context, so slow runners cannot reject a real win merely because the banner's display timer elapsed.

- PR #11 physical-evasion proof completion on 2026-08-31: route defense can no longer treat telegraph resolution itself as a successful evade; held movement ends only after the authoritative snapshot proves player/enemy separation exceeds the attack range, then independently confirms the telegraph has resolved.

- PR #11 sixteenth-review camera-relative evasion on 2026-08-31: hosted route defense now transforms W/A/S/D candidates through the snapshot's live lock-on camera yaw before ranking projected arena safety and enemy separation, so every chosen key matches its actual world-space displacement.

- PR #11 hosted route physical-evasion strategy on 2026-08-30: after all 21 non-route browser scenarios passed, the victory driver replaced stamina-dependent queued dodges with ordinary collision-aware movement selected from safe arena-interior candidates. Each defense stops moving as soon as authoritative player/enemy separation exceeds the exact melee or area range, then waits for telegraph resolution; the dedicated controls scenario continues to prove block and dodge behavior independently.

- PR #11 fifteenth-review dodge-state proof on 2026-08-30: queued-dodge direction is now released only after the authoritative 360 ms `dodge` action is observed, removing ordinary held-key locomotion as a false-positive start signal.

- PR #11 fourteenth-review queued-dodge evidence on 2026-08-30: the hosted route keeps its selected lateral key held pending authoritative dodge-start evidence, ensuring a dodge buffered behind an attack still samples the intended safe direction on capped slow frames.

- PR #11 hosted combat-route stabilization on 2026-08-30: the deterministic route now advances from the aggro edge into the arena interior before fighting, selects lateral dodges toward the center or away from the bandit, and holds dodge/block defense until the authoritative telegraph resolves instead of assuming wall-clock timing. Current hosted evidence passes 21 of 27 scenarios, with only the prior boundary-crossing route driver failing across the three profiles.

- PR #11 thirteenth-review clock synchronization on 2026-08-30: Babylon animation groups now use a per-frame scene time scale derived from the exact capped combat delta and freeze while paused, so slow mobile frames cannot advance visible attacks, dodges, reactions, or defeat motion ahead of authoritative gameplay. A focused regression covers normal, capped, paused, and zero-delta clocks.

- PR #11 twelfth-review animation timing on 2026-08-30: every state-bounded one-shot actor clip now derives its playback ratio from the authored clip length and the same authoritative attack, dodge, hit, stagger, recovery, guard-break, or defeat duration that advances gameplay, keeping visible motion and hit resolution synchronized.

- PR #11 current-head CI scheduling on 2026-08-30: pull-request workflow runs now cancel obsolete in-progress executions so a known-bad predecessor cannot block authoritative current-head browser evidence; main-branch Pages builds remain serialized.

- PR #11 hosted-ray registration fix on 2026-08-30: line-of-sight validation now imports Babylon's side-effectful Ray entry point so hosted production registers the picking implementation before `Scene.pickWithRay`; the prior core-only import surfaced a runtime diagnostic and prevented all authoritative strike resolution in Chromium.

- PR #11 eleventh-review hardening on 2026-08-30: browser evidence now captures `PATH UNLOCKED` while the victory feedback is visibly rendered before independently waiting for the physical gate, and world reset clears feedback timers/content, hit-flash state, and all live damage-number elements so no prior-combat effects leak into the fresh spawn.

- PR #11 hosted-browser combat hardening on 2026-08-30: current predecessor evidence passed 18 of 27 scenarios and isolated the remaining failures to transient combat timing. The control scenario now waits for attack recovery before measuring held block and uses the keyboard dodge path; the legitimate driver checks telegraphs every 90 ms, advances combos one authoritative strike at a time, alternates measured lateral dodges for heavy/area attacks, holds basic guard through impact, and uses a bounded 180-second profile budget.

- PR #11 tenth-review hardening on 2026-08-30: player strikes now require the same authoritative unobstructed collision-geometry ray as enemy hits before either bandit dodge or damage can resolve, preventing melee through cottage and wall corners.

- PR #11 ninth-review hardening on 2026-08-30: bandit chase and dodge displacement now use collision-aware movement with an enabled enemy collider, enemy damage additionally requires an unobstructed collision-geometry ray, and disengagement clears both stagger and dodge reaction state while completed staggers return the actor to guard idle.

- PR #11 eighth-review hardening on 2026-08-30: the bandit's alternating third-hit dodge is now an explicit short state that cancels and advances any active telegraph before playing Roll without same-frame locomotion replacement, and identical consecutive non-looping attack clips restart so buffered combo boundaries remain visually synchronized.

- PR #11 seventh-review hardening on 2026-08-30: the Bio's collision center is bounded inside the 42-meter ground footprint with capsule clearance, preventing traversal around the outer endpoints of the combat-gate flanks, and every enemy telegraph ring now derives its visible outer radius from the exact basic/heavy or area hit-validation range.

- PR #11 sixth-review hardening on 2026-08-30: collision-enabled stone flanks now span the world boundary around the closed combat gate so the village cannot be entered around its sides, damaging hit reactions clear active dodge time and velocity, and narrow portrait layouts stack the Pause/Reset actions above the HUD without obscuring the status panel.

- PR #11 fifth-review hardening on 2026-08-30: pause requests are consumed but ignored during the defeat delay so its automatic reset cannot be hidden behind an inaccessible overlay, and damaging hits now hold an explicit short reaction state that cancels an interrupted attack and keeps the Bio's hit animation visible before locomotion resumes.

- PR #11 fourth-review hardening on 2026-08-30: guarded player hits preserve any active bandit attack animation through its still-authoritative impact, the area telegraph outer radius is derived from the same 2.75-meter range used for hit validation, and input clearing on pause, focus loss, or reset also discards queued attack, dodge, and pause actions.

- PR #11 third-review hardening on 2026-08-30: player attacks retain their real pre-spend stamina so exhausted damage is scaled exactly once, active enemy telegraphs persist through impact even when the Bio crosses the normal melee boundary, and stopped movement returns the visible Bio to the appropriate idle animation.

- PR #11 hosted-browser hardening on 2026-08-30: the first intact-asset Chromium run loaded both character models and passed 16 scenarios, exposing only automation timing/strategy failures. Collision evidence now waits for actual wall contact across emulation speeds, dodge evidence uses durable stamina and displacement outcomes instead of sampling a transient action frame, and the legitimate combat driver reserves a full-strength combo budget while dodging heavy/area attacks instead of exhausting itself into defeat.

- PR #11 second-review hardening on 2026-08-30: the route-completion destination now lies beyond the physical combat gate, with a regression proving the approach side cannot complete the scenario, and the bandit's guard-break stagger uses the Warrior asset's existing `RecieveHit` clip.

- PR #11 first-review hardening on 2026-08-30: combat evidence now reports the gate open only after its physical rise disables collision, held blocking suppresses stamina regeneration so its eight-second budget drains, and the sword bandit plays its actual `Sword_Attack` clips. Character GLBs now use baseline embedded JPEG textures without decoder-dependent glTF texture extensions, asset-load failures surface directly in browser assertions, and regressions cover held-block drain plus physical gate readiness. Lint, typecheck, 12 unit tests, production build, H2 specification checks, 23 bridge tests, and the complete H1 gate pass locally; hosted Chromium remains the authoritative browser run.

- H2 combat and System UI slice on 2026-08-30: the operator completed a one-question-at-a-time combat design interview. The implementation adds a tap-driven three-hit unarmed combo, one-action input buffering, automatic target lock and strafing, freely moving attacks with a bounded closing step, shared stamina for attacks/block/dodge, severe exhausted actions, guard breaks, physical-distance dodges, readable basic/basic/heavy/area sword-bandit attacks, predictable block/dodge defense, health/stamina/enemy bars, hit flash and damage numbers, synthesized sound feedback, pause/defeat/reset flow, and a combat-gated path. Free CC0 Quaternius Monk and Warrior models are bundled locally with source/license evidence. Pure combat rules, browser scenarios, and the `h2-browser-combat-ui` gate now cover the agreed loop across desktop, folded-phone, and unfolded-landscape profiles.

- H2 mobile-route time-budget correction on 2026-08-30: the complete route scenario now has a 75-second outer ceiling while retaining its 10-second per-movement bounds. Production evidence showed the unfolded mobile retry reach `complete` with no runtime errors before the prior 45-second test ceiling interrupted key release.

- H2 mobile-route evidence stability on 2026-08-30: the bounded road-centering helper now allows slower mobile emulation enough time to cover the same measured lateral distance as desktop. The production gate exposed the prior three-second ceiling when folded and unfolded profiles stopped just outside the route tolerance without runtime or collision errors.

- H2 production-control verification on 2026-08-30: the Pages workflow now stamps the tested commit into its deployment artifact and, after publishing, reruns the complete deterministic gameplay suite against the real public URL in WebGL-capable desktop, folded-phone, and unfolded-phone Chromium profiles. Production evidence must match the deployed revision and covers rendering, movement, camera control, collision, reset, lifecycle recovery, touch input, and complete route traversal.

- H2 GitHub Pages deployment slice on 2026-08-30: the existing H2 workflow now packages `web/dist` only after the complete web-foundation job succeeds on `main`, then deploys that immutable artifact through the protected `github-pages` environment. Pull requests continue to build and test without publishing, while a manual `main` dispatch can safely republish the same tested path.

- H2 phone-first client foundation on 2026-08-29: at operator direction, the active graphical client moved from an unavailable dedicated Unreal host to Babylon.js mobile web while preserving the validated engine/World OS boundary and deferred Unreal option. The first focused slice adds a deterministic Albion greybox, Bio movement, keyboard/pointer and touch input, blocking collision, reset/route feedback, production static build, unit checks, and Playwright evidence across desktop, folded-phone, and unfolded-landscape viewports.

- PR #4 pre-versioned Ed25519 compatibility on 2026-08-28: schema-v1 bridge snapshots now distinguish genuine legacy 64-hex HMAC proofs from the immediately preceding 128-hex Ed25519 format. Legacy shared-key outcomes still fail explicitly, while already-valid Ed25519 decisions verify under the configured engine public key and migrate to bridge persistence schema v2.

- PR #4 sixteenth review hardening on 2026-08-28: engine snapshots now persist immutable initial actor positions; restore replays every applied destination from that genesis baseline and compares the complete physical position map, including actors that never moved.

- PR #4 fifteenth review hardening on 2026-08-28: restored applied decisions must match their authenticated proposal action fields, while physical positions and derived last-action state must match the ordered authoritative event history.

- PR #4 fourteenth review hardening on 2026-08-28: processed authority records now persist the complete signed World OS proposal; restore revalidates its origin proof, identity, and digest, then derives actor sequence high-water marks exclusively from those authenticated envelopes.

- PR #4 thirteenth review hardening on 2026-08-28: each processed engine proposal now persists its actor and proposal sequence, and restored per-actor high-water marks must exactly match that processed ledger before the authority can accept new work.

- PR #4 twelfth review hardening on 2026-08-28: retry delivery groups now persist drained observation identities separately from emitted proposals, preserving valid zero-proposal observations through reload, and every restored applied observation is revalidated against the live engine-observation contract.

- PR #4 eleventh review hardening on 2026-08-28: restored delivery results now bind exact canonical pending proposals to applied observation correlations, bridge outcomes reject reused ordering slots, and authoritative engine-event state versions require exact non-boolean integers. Focused regressions cover tampered retry envelopes, separate-authority outcome collisions, and boolean/float event versions.

- PR #2 ninth review hardening on 2026-08-24: awakening transitions and their independent goals now use the authoritative internal proposal path; validation recomputes contributor identity, Bio provenance, distinct ticks, threshold score, cognition, location, and payload evidence before either mutation becomes durable. Thinker genesis goals use the same validated path. Fifteen focused tests, the full H1 gate, and the complete H0 regression gate passed.
- PR #2 eighth review hardening on 2026-08-24: request actions now require an explicit physical-possibility rule before application, and conscious decisions parent both the triggering request and the exact event-backed goals consulted, so refusal traces reach awakening/genesis goal causes. Fifteen focused tests, the full H1 gate, and the complete H0 regression gate passed.
- PR #2 seventh review hardening on 2026-08-24: world-pressure and actor-action proposals now pass through one authoritative internal validator with durable permission, precondition, World Anchor, and playability evidence; native Thinkers receive deterministic event-backed genesis goals, and decisions consult persistent goals as well as values. Fifteen focused tests, the full H1 gate, and the complete H0 regression gate passed.
- PR #2 sixth review hardening on 2026-08-24: parent-only interactions now consume a canonical causal-parent identity, allowing genuinely new chains while rejecting replay of one parent across ticks; duplicate parent identities are rejected at the proposal boundary. Fifteen focused tests, the full H1 gate, and the complete H0 regression gate passed.
- PR #2 fifth review hardening on 2026-08-24: causal-parent-only meaningful interactions now deduplicate only when a root input actually exists, and proposal boundary validation rejects malformed actor, participant, parent, location, root-input, event-type, and payload shapes as durable evidence instead of raising. Fifteen focused tests, the full H1 gate, and the complete H0 regression gate passed.
- PR #2 fourth review hardening on 2026-08-24: conscious Thinkers now use values-based agency instead of routine responses, and malformed rumor provenance is rejected as inspectable evidence before conversion. Fourteen focused tests, the full H1 gate, and the complete H0 regression gate passed.
- PR #2 third review hardening on 2026-08-24: the externally trusted persistence digest now binds the complete state/event boundary, rejected requests cannot emit follow-on responses, replay preserves all accepted rumor metadata, and malformed non-string interaction factors produce durable rejection evidence instead of exceptions. Fourteen focused tests, the full H1 gate, and the complete H0 regression gate passed.
- PR #2 second review hardening on 2026-08-24: every rumor share now requires the exact causal knowledge parent selected for its provenance, awakening evidence rejects reused roots and must span three logical ticks, and persistence requires a caller-supplied trusted genesis digest covering seed, actors, and crisis authority. Fourteen focused tests, the full H1 gate, and the complete H0 regression gate passed.
- PR #2 review hardening on 2026-08-24: rejection evidence now isolates invalid proposed parents, forwarded rumors parent the actual received-rumor event, awakening requires at least three distinct-factor Bio interactions, persistence replays the complete causal history instead of trusting a recomputable digest, each Bio receives deterministic immediate Earth-memory evidence, and relationship traces expose per-event rule contributions. Thirteen focused tests, the full H1 gate, and the complete H0 regression gate passed.
- H1 Text Simulator implemented on 2026-08-24 as a standard-library-only deterministic runtime. Seeded tests cover immutable append-only events, perspective memories/beliefs, multidimensional evidence-derived relationships, traceable rumor propagation, role routines, emergent awakening, retained history, independent goals/refusal, autonomous crisis evolution, stable proposal ordering, validated rejection evidence, versioned persistence, tamper/causal-integrity rejection, replay equality, and machine-readable causal traces. All six H1 gate checks passed.
- H0 deliberately closed on 2026-08-24 at merged main `3ba24b951d6fbbb497522cd739a785e781d409b7`: `python3 bin/milestone-gate.py --json` passed all six required checks on the development machine. The active milestone moved to H1 Text Simulator under its previously finalized specification and evidence manifest.
- Harness conversion starter created: protected laws, milestone contract, acceptance tests, Codex structured runner, progress health probe, and simulation/Unreal architecture split.
- Codex lifecycle foundation added: explicit workspace-write sandbox, JSONL/stderr separation, persisted thread resume, child PID tracking, last-run metadata, single-instance supervisor with exponential backoff, pause/stop controls, race-safe restart, and static H0 checks.
- Codex PR review findings addressed: atomic runner/supervisor PID locks, streaming thread-id persistence, fully drained event streams, collision-resistant run logs, corrected lifecycle invocation counting, expanded H0 control-plane coverage, and fail-closed gate configuration errors.
- PR #1 timeout-tree review fix: gate escalation now targets the POSIX process group even after its direct leader exits, and the post-SIGKILL pipe drain is bounded/fail-closed. The regression reproduces a leader that exits on TERM plus a descendant that ignores TERM/HUP and retains inherited pipes. `python3 bin/milestone-gate.py --json` passed all five H0 checks on 2026-08-13.
- PR #1 redirected-output follow-up: timeout cleanup now preserves the complete TERM grace period and always sends process-group SIGKILL afterward, even when the leader exits and stdout/stderr reach EOF. A second regression redirects a stubborn descendant's streams to `/dev/null` and proves it cannot perform a delayed worktree write. The complete five-check H0 gate passed again on 2026-08-13.
- PR #1 wrapper/restart follow-up: the Codex process wrapper now preserves its own full TERM grace period before an unconditional process-group SIGKILL, persists its PID, and watches a restart stop-control file. Restart can therefore recover the inherited runner lock after the runner shell is SIGKILLed without signaling recycled PID metadata. Deterministic redirected-descendant and orphaned-wrapper regressions pass; the complete five-check H0 gate passed on 2026-08-13.
- PR #1 zombie-wrapper test follow-up: orphan recovery evidence now verifies that restart installs a distinct live wrapper holding the authoritative runner lock instead of treating `kill -0` signalability as proof the old orphan still executes. This permits an exited zombie awaiting PID 1 reaping without weakening recovery coverage. The focused control-plane suite and complete five-check H0 gate passed on 2026-08-13.
- PR #1 replacement-wrapper timing follow-up: the orphan-recovery regression now polls with a bounded timeout for the replacement Python wrapper after restart reports runner-lock ownership, covering the legitimate shell-to-wrapper startup interval without weakening the distinct-live-wrapper assertion. The focused control-plane suite and complete five-check H0 gate passed on 2026-08-13.
- PR #1 consolidated lifecycle audit: restart now uses runner and wrapper kernel locks rather than PID signalability as liveness/exit evidence, reports success only after a matching wrapper/child readiness handshake, captures signals across wrapper startup, and falls back to whole-process-group cleanup if the wrapper is SIGKILLed. Deterministic coverage now includes zombies, immediate repeated restart, concurrent restart rejection, readiness handoff, and escaped descendants. The expanded control suite passed three consecutive stress runs; after the final wrapper-lease hardening, the focused suite and complete five-check H0 gate passed again on 2026-08-13. All runtime locks were free afterward.
- PR #1 replacement-identity follow-up: restart readiness now requires the runner-lock PID to equal the exact shell spawned by that restart and requires that child to remain a running shell job. A deterministic regression kills the intended handoff, starts an unrelated healthy direct runner in the same polling window, and proves restart fails instead of claiming the unrelated runner. The focused control suite and complete five-check H0 gate passed on 2026-08-13.
- PR #1 supervisor-lease follow-up: the supervisor now closes its private lock descriptor in the background runner before exec, so supervisor SIGKILL cannot leave a healthy or hung runner holding a false supervisor lease. A deterministic regression keeps the runner alive after supervisor death and proves a replacement supervisor can immediately acquire the lease. The focused control suite and complete five-check H0 gate passed on 2026-08-13.
- PR #1 private-lease completion: every supervisor child now excludes fd 7, gate evaluation also excludes the parent-held control/runner descriptors, and every restart helper excludes fd 6. Both note and no-note restarts share one replacement launch, which retains only the intentional fd 8/9 handoff. Deterministic SIGKILL regressions cover supervisor backoff, gate evaluation, restart control-lock wait, and replacement startup; the focused suite passed three parallel stress runs and the complete five-check H0 gate passed on 2026-08-14.
- PR #1 gate-serialization correction: the gate evaluator now excludes only supervisor-private fd 7 and intentionally retains the fd 8/9 critical-section leases until evaluation finishes. Supervisor handoff release is close-only, so both SIGKILL and graceful SIGTERM preserve serialization without extending the private supervisor lease. The focused suite passed three parallel stress runs and the complete five-check H0 gate passed on 2026-08-14.
- PR #1 restart-note test correction: the gate/restart race regression now proves restart is blocked on the gate-held control lock, releases the gate, waits for successful replacement readiness, and only then checks the resumed note trace. This removes a false failure window during valid slow recovery without weakening note-delivery coverage. The focused control suite and complete five-check H0 gate passed on 2026-08-14.
- PR #1 H1-contract completion: H1 Text Simulator now has finalized acceptance criteria, an explicit deterministic/domain/evidence specification, and a future executable manifest whose missing implementation checks keep H1 safely incomplete. H0 now requires a static H1 contract check that verifies the specification and exact evidence commands. The future H1 gate evaluated as valid-but-incomplete (exit 1), and the expanded six-check H0 gate passed on 2026-08-15.
- PR #1 evaluator-lease hardening: the supervisor now explicitly delegates only its control/runner descriptors to each active milestone check, preserving serialization if `milestone-gate.py` is SIGKILLed. The adjacent completed-leader case is fail-closed: any same-session background process is terminated and makes the check fail. Runner handoff also drops and reacquires fresh locks after an incomplete gate, so even a lease-holding descendant that escaped with `setsid` cannot overlap Codex through the shared open description. Deterministic evaluator-death, redirected-background, and escaped-session regressions pass; the complete six-check H0 gate passed on 2026-08-15. Product laws and acceptance criteria are unchanged.
- PR #1 normal-exit/zombie follow-up: Codex wrapper cleanup now terminates executable process-group descendants after normal as well as interrupted leader exits, preventing redirected background work from outliving the runner lease. Shared Linux process-state detection uses namespace-aware `NSpgid` and ignores zombie-only groups, preventing harmless delayed PID 1 reaping from failing gate evidence. Regressions cover delayed normal-exit worktree mutation, synthetic live/zombie states, and a successful check that leaves an orphan zombie; the complete six-check H0 gate passed on 2026-08-15.
- PR #1 pre-join/namespace-depth follow-up: normal Codex leader completion now cleans its executable process group before joining stdout/stderr consumers, so a stubborn descendant retaining inherited pipes cannot hang the wrapper or runner lease. Process-group discovery derives the caller's namespace index from procfs self metadata rather than assuming either endpoint of `NSpgid`, covering both host callers and nested-container callers. Targeted lifecycle and gate-timeout regressions passed, followed by the complete six-check H0 gate on 2026-08-15 (static, H1 contract, lifecycle, control, lock-race, and timeout-tree checks all passed).
- PR #1 detached-descendant completion: the runner wrapper and milestone evaluator now become Linux child subreapers and terminate their complete namespace-aware executable descendant trees, including `setsid` and double-fork escapes. The runner shell supplies a second subreaper cleanup boundary if the Python wrapper is killed, while successful checks that leave detached work are rejected and cleaned before their evidence can pass. Deterministic regressions cover detached Codex children, successful detached checks, evaluator cleanup, and hard wrapper death; the final six-check H0 gate passed on 2026-08-16.
- PR #1 persistent-evaluator-owner follow-up: the supervisor is now the outer gate child subreaper, so evaluator SIGKILL cannot release serialization while a detached check child that closed every inherited descriptor remains executable. The supervisor adopts and terminates the tree, rejects the gate as infrastructure-failed, and only then releases its control/runner leases. The runner also removed post-cleanup signaling through stale numeric process-group metadata. A deterministic close-fds/setsid evaluator-death regression and the complete six-check H0 gate passed on 2026-08-16; a follow-up audit kept supervised runners on their own distinct subreaper boundary.
- PR #1 persistent gate-owner completion: supervised evaluation now runs behind a distinct lease-holding subreaper/watchdog, so supervisor/evaluator double death cannot orphan descriptor-closing work. Interrupted cleanup is retried without releasing either lease and leaves a persistent quarantine; stale active ownership blocks runner, restart, and supervisor entry points. Deterministic double-death, killed-cleanup-helper, lock-retention, and stale-marker regressions passed, followed by all six H0 checks through the integrated supervisor/watchdog path on 2026-08-23.
- PR #1 cleanup-handshake follow-up: the watchdog now records its exact gate-guard PID, letting the compound-failure regressions stop that guard before killing the evaluator and deterministically observe the injected strict-cleanup helper. The focused control suite passed once plus three consecutive stress runs on 2026-08-23.
- H1 merged-main closure: PR #2 was squash-merged as `249aaf7b069c82ff0c001c614f686746cfafbbb2` after a clean hosted Codex review. All six H1 checks and the complete H0 regression gate passed again on merged `main` on 2026-08-28; the evidence is recorded in `milestones/H1/COMPLETION.md`.
- H2 contract start: Engine Prototype scope is limited to one playable Albion village greybox, a versioned fail-closed World OS/Unreal bridge, movement, simple combat, minimum System UI, visibly distinct Thinker/Non-Thinker behavior, deterministic PIE automation, and inspected visual evidence. Awakening and content-scale work remain outside H2.
- H2 bridge slice started: added a versioned JSON envelope, idempotent engine-observation ingestion, World OS-originated routine and Thinker-choice proposals, a reference engine authority with atomic applied/rejected outcomes, and deterministic evidence for success, duplicate, stale, impossible, unauthorized, and message-id-conflict paths.
- PR #4 first review hardening on 2026-08-28: bridge retry/proposal/outcome ledgers now persist atomically inside the trusted world snapshot, proposals carry keyed World OS origin proofs, conflicting proposal payloads return the already-canonical engine outcome while recording conflict evidence, and authoritative engine-event identities reject different-content reuse. Five focused bridge tests, all 20 repository tests, the complete H1 regression gate, the H2 specification check, and the complete H0 harness regression gate passed.
- PR #4 second review hardening on 2026-08-28: unauthenticated proposals no longer consume actor sequence state, the engine authority persists physical state and its processed-proposal/canonical-outcome ledger in one trusted snapshot, engine outcomes require exact status-specific payload fields, and worlds omit empty extensions so pre-extension H1 snapshots retain their verified digest after load. Restart and forged-high-sequence regressions pass with the full repository suite.
- PR #4 third review hardening on 2026-08-28: applied engine state versions are now durably bound to one authoritative engine-event digest, preventing separate or incorrectly restored engine instances from returning different physical mutations at the same version. A two-authority version-reuse regression was added alongside the existing event-ID reuse coverage.
- PR #4 fourth review hardening on 2026-08-28: authoritative engine events now carry the complete ordered digest lineage of prior events, allowing out-of-order delivery to reserve one coherent history and reject forked predecessor events. Invalid-origin envelopes no longer emit correlatable outcomes before a genuine proposal is processed; after processing, ID collisions can only receive the canonical existing outcome.
- Engine-to-World OS outcomes now also carry a keyed authority proof over the complete outcome and optional authoritative event, so a structurally valid but forged rejection cannot preempt the real engine decision for a pending proposal.
- PR #4 fifth review hardening on 2026-08-28: unseen higher engine-observation sequences are durably buffered per actor until every predecessor arrives, then applied once in logical order with retryable proposal delivery results. Bridge schema versions now reject booleans explicitly instead of relying on Python equality with integer version 1.
- PR #4 sixth review hardening on 2026-08-28: persisted bridge snapshots from the immediately preceding H2 shape migrate by adding empty reorder/delivery collections and are rewritten in the current shape. Persisted bridge and engine-authority schema versions now enforce the same strict non-boolean integer check as live envelopes.
- PR #4 seventh review hardening on 2026-08-28: persisted ordering counters now require exact non-boolean integer types and are cross-checked against contiguous observation/proposal ledgers. Engine state/decision counters are likewise checked against the complete applied-event history and processed-decision ledger before a restored authority can mutate physical state.
- PR #4 eighth review hardening on 2026-08-28: restored engine decisions must occupy exactly the unique contiguous sequence `1..decision_sequence`, applied state versions cannot repeat before history reconstruction, processed proposal identities cannot duplicate, and every persisted decision's keyed authority proof is revalidated.
- PR #4 ninth review hardening on 2026-08-28: list-backed bridge ledgers now reject duplicate observation, proposal, outcome, buffered-message, and normalized engine-version identities before rebuilding maps, preventing digest-valid snapshots from silently dropping entries.
- PR #4 tenth review hardening on 2026-08-28: restored applied and buffered observation ledgers must have disjoint message identities, so an out-of-order delivery cannot overwrite an already-applied observation after restart.
- PR #4 eleventh review hardening on 2026-08-28: restored applied observations and pending proposals must occupy unique contiguous per-actor sequence slots. Engine event/version indexes are rebuilt from authenticated applied decisions and their full lineage and must exactly match the persisted derived maps.
- PR #4 authority replay hardening on 2026-08-28: authenticated engine proposals that arrive ahead of an actor's next sequence are durably buffered and drained in order after the gap closes, including across restart. Engine-authority snapshots now have their own version and safely migrate the immediately preceding shape, while restored decisions are replayed through the persisted permissions, destinations, and blocked-path policy before any state is trusted.
- PR #4 buffered-outcome completion on 2026-08-28: authority validation now returns an ordered decision batch, so closing a proposal-sequence gap emits the outcome for the gap-closing proposal and every newly drained proposal in the same call. No applied physical mutation can be hidden behind an empty or partial transport response.
- PR #4 response-retry completion on 2026-08-28: the complete ordered decision batch is now persisted under its triggering proposal identity and replayed exactly when that request is retried, including after reload. Authority schema v3 migrates only unambiguous v2 histories and fails closed when an older multi-command actor history cannot prove its original response boundaries.
- PR #4 legacy-migration hardening on 2026-08-28: malformed v1/v2 processed records are now normalized to `BridgeValidationError` inside the loader's fail-closed boundary instead of leaking raw Python lookup/type errors during response-batch reconstruction.
- PR #4 v1-history compatibility on 2026-08-28: schema-v1 processed decisions are policy-replayed with their historical immediate high-sequence semantics, preserving legitimate out-of-order v1 snapshots during migration. Once restored, every new proposal uses the current buffered v3 ordering contract.
- PR #4 real-gap validation on 2026-08-28: restored bridge observations and authority proposals may be buffered only beyond the actor's next expected sequence. Digest-valid snapshots that strand sequence 1 (or any immediately processable successor) in a buffer now fail closed instead of permanently deadlocking that actor stream.
- PR #4 engine trust separation on 2026-08-28: engine outcomes now carry Ed25519 signatures made with an engine-only private key; World OS receives only the public verification key. Possessing the separate proposal-origin secret can no longer fabricate an authoritative applied/rejected result.
- PR #4 signature-format transition on 2026-08-28: bridge persistence and engine-authority persistence now version the Ed25519 boundary separately from live envelope versions. The engine can verify and re-sign trusted legacy HMAC decisions during authority migration; World OS snapshots containing legacy outcomes fail early with an explicit incompatible-signature error because World OS correctly lacks the engine private key.

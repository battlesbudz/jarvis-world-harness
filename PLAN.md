# Agent Plan

1. Reproduce normal-exit Codex descendant leakage and zombie-only gate false positives.
2. Centralize executable process-group detection and apply it to both wrapper and gate cleanup.
3. Add deterministic normal-exit descendant and zombie-state regressions, then stress the focused lifecycle suites.
4. Run the complete H0 gate, record evidence, publish the fix, and request a fresh Codex review.
